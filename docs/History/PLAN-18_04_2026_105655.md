# PLAN - Migliorie pagina di registrazione

## Stato Generale

- PRD: aggiornato in `docs/PRD.md`.
- Piano: aggiornato in `docs/PLAN.md`.
- Fase corrente: Fase 4 completata. Piano tecnico completato.
- Regola operativa: procedere per fasi piccole e validabili, aggiornando il piano dopo ogni fase.

## Checklist Generale

- [x] Leggere `AGENTS.md` e moduli `_shared`.
- [x] Applicare la skill `dotnet-task-decomposition` su richiesta esplicita.
- [x] Aggiornare `docs/PRD.md` per le migliorie della pagina di registrazione.
- [x] Aggiornare `docs/PLAN.md` per le fasi del nuovo task.
- [x] Completare Fase 1 - Analisi pagina registrazione e toast.
- [x] Completare Fase 2 - Implementazione UI e comportamento.
- [x] Completare Fase 3 - Test e verifiche.
- [x] Completare Fase 4 - Revisione finale e archiviazione documenti.

## Fase 1 - Analisi pagina registrazione e toast

### Obiettivo

Individuare con precisione file, markup, model, controller, script e stili coinvolti prima di modificare il codice applicativo.

### Stato

Completata.

### Attivita

- [x] Verificare `git status --short`.
- [x] Leggere `Views/Account/Register.cshtml`.
- [x] Leggere `Models/Register.cs` e `Controllers/AccountController.cs`.
- [x] Leggere `Views/Shared/_ToastMessages.cshtml`.
- [x] Verificare eventuali script o stili esistenti collegati alla pagina.
- [x] Identificare test esistenti su registrazione e controller account.

### File o Aree Coinvolte

- `Views/Account/Register.cshtml`
- `Models/Register.cs`
- `Controllers/AccountController.cs`
- `Views/Shared/_ToastMessages.cshtml`
- `Styles/app.css`
- `wwwroot/css/app.css`
- `DashboardOrders.Tests/`

### Validazioni

- Nessuna modifica codice in questa fase.
- Conferma dei file da toccare e delle verifiche necessarie.

### Definition of Done

- Il perimetro tecnico e chiaro.
- Il piano viene aggiornato con eventuali note emerse dall'analisi.

### Note

- Evitare regex complesse con virgolette escaped in PowerShell, come indicato in `AGENTS.md`.
- `Views/Account/Register.cshtml` contiene tutto il markup del form; attualmente usa una griglia `md:grid-cols-2`, ma `RepeatPassword` finisce sulla riga successiva perche `Cap` occupa la colonna prima di `Password`.
- `AccountController.Register()` restituisce `new Register()` e quindi `DateOfBirth` resta `default`, causando la visualizzazione di `01/01/0001`.
- `AccountController.ValidateRegister()` oggi rifiuta date uguali o successive a `DateTime.Today`; questo e in conflitto con il requisito di mostrare la data odierna come valore iniziale se l'utente invia senza modificarla.
- `Models/Register.cs` definisce tutti i campi del form come obbligatori tramite `Required`.
- `Views/Shared/_ToastMessages.cshtml` e la partial condivisa per toast success/error; va aggiornata con attributi o classi usabili da JavaScript per auto-close e click-close.
- `wwwroot/js/site.js` contiene solo logica di ricerca pagina; si puo estendere con logica generica per toast e form registrazione senza nuove dipendenze.
- `Styles/app.css` contiene solo direttive Tailwind; salvo necessita, le modifiche possono restare in classi Tailwind dentro le view e richiedere rigenerazione di `wwwroot/css/app.css`.
- `DashboardOrders.Tests/AccountControllerTests.cs` copre GET/POST register e va aggiornato almeno per verificare che GET register inizializzi la data odierna.
- Non risultano test automatici di rendering Razor o JavaScript; per comportamento client serve smoke/manuale o browser automation nella fase verifiche.

### File Modificati

- `docs/PLAN.md`: aggiornata analisi tecnica della Fase 1.

### Validazione Eseguita

- `git status --short`: presenti modifiche documentali gia attese a `docs/PRD.md` e `docs/PLAN.md`.
- Lettura statica di view, model, controller, partial toast, layout, script, configurazione Tailwind e test account.

### Decisioni Prese

- La Fase 2 dovra modificare `AccountController.cs` per inizializzare la data odierna su GET e, con attenzione, gestire il caso POST con data lasciata al valore iniziale.
- La logica client per abilitare `Crea Account` puo essere inserita nello script della view o in `wwwroot/js/site.js`; la scelta verra fatta in base al minor impatto durante l'implementazione.
- I toast saranno aggiornati nella partial condivisa per mantenere lo stesso comportamento su tutte le pagine.

## Fase 2 - Implementazione UI e comportamento

### Obiettivo

Applicare le modifiche richieste alla pagina di registrazione e ai toast mantenendo validazioni server e comportamento MVC esistenti.

### Stato

Completata.

### Attivita

- [x] Impostare la data odierna come valore iniziale di `Data di nascita`.
- [x] Allineare `Password` e `Ripeti password` sulla stessa riga in desktop e mantenerli responsive su mobile.
- [x] Aggiungere `*` alle label dei campi obbligatori.
- [x] Aggiornare header card con `Dati account` a sinistra e `* campi obbligatori` a destra in rosso.
- [x] Disabilitare `Crea Account` finche tutti i campi obbligatori non sono valorizzati.
- [x] Aggiornare toast con chiusura automatica dopo 5 secondi e chiusura al click.
- [x] Rendere i toast di errore piu visibili con sfondo rosso e testo bianco bold.

### File o Aree Coinvolte

- `Views/Account/Register.cshtml`
- `Controllers/AccountController.cs`
- `Models/Register.cs`
- `Views/Shared/_ToastMessages.cshtml`
- `Styles/app.css`
- `wwwroot/css/app.css`

### Validazioni

- `dotnet build DashBoard01.sln`
- `npm run build:css` se vengono toccate classi Tailwind, `Styles/app.css` o view rilevanti.
- Verifica manuale o smoke della pagina registrazione.

### Definition of Done

- Tutti i requisiti UX del PRD sono implementati.
- Le modifiche sono coerenti con MVC/Razor/Tailwind esistenti.
- Le validazioni server restano attive.

### Note

- Il pulsante disabilitato lato client e un supporto UX, non sostituisce la validazione lato server.
- `AccountController.Register()` ora usa un view model iniziale con `DateOfBirth = DateTime.Today`.
- La validazione server della data blocca solo date future; la data odierna resta accettata per coerenza con il requisito di valore iniziale.
- `Views/Account/Register.cshtml` usa label esplicite con `*`, header card responsive e attributi `data-register-*` per la logica client.
- `Codice fiscale` occupa due colonne in desktop, cosi `Password` e `Ripeti password` restano sulla stessa riga.
- `Views/Shared/_ToastMessages.cshtml` usa `data-toast` per chiusura click/auto-close e stile errore rosso con testo bianco bold.
- `wwwroot/js/site.js` e stato riorganizzato in funzioni di inizializzazione per ricerca pagina, toast e form registrazione.

### File Modificati

- `Controllers/AccountController.cs`: inizializzazione data odierna e validazione data futura.
- `Views/Account/Register.cshtml`: label obbligatorie, header card, layout password e attributi submit client.
- `Views/Shared/_ToastMessages.cshtml`: stile toast errore e attributi per chiusura.
- `wwwroot/js/site.js`: logica toast auto-close/click-close e abilitazione submit registrazione.
- `docs/PLAN.md`: aggiornata esecuzione della Fase 2.

### Validazione Eseguita

- `git status --short`: verificato prima delle modifiche applicative.
- Rilettura statica dei file modificati per confermare markup, attributi e logica JavaScript.

### Decisioni Prese

- Il requisito di data odierna come default prevale sulla precedente regola "data nel passato"; le date future restano non valide.
- La logica di abilitazione del pulsante usa tutti i campi marcati con `data-register-required`, allineandosi ai campi `Required` del model.
- La logica toast e globale perche la partial e condivisa da piu pagine.

## Fase 3 - Test e verifiche

### Obiettivo

Confermare che le modifiche non introducano regressioni e aggiungere test dove la logica e verificabile automaticamente.

### Stato

Completata.

### Attivita

- [x] Aggiornare o aggiungere test controller/model se viene modificata inizializzazione o logica server.
- [x] Eseguire `dotnet test DashBoard01.sln`.
- [x] Eseguire `dotnet build DashBoard01.sln`.
- [x] Eseguire `npm run build:css` se pertinente.
- [x] Eseguire smoke test della pagina registrazione, inclusi stato iniziale, compilazione campi e toast.

### File o Aree Coinvolte

- `DashboardOrders.Tests/`
- File applicativi modificati nella Fase 2.

### Validazioni

- Test automatici .NET.
- Build .NET.
- Build CSS.
- Smoke UI su desktop e mobile quando possibile.

### Definition of Done

- Le verifiche pertinenti passano.
- Eventuali failure vengono corrette prima di procedere.

### Note

- Se non viene aggiunta logica server nuova, i test possono limitarsi ad aggiornare coperture esistenti e a verifiche build/smoke.
- `DashboardOrders.Tests/AccountControllerTests.cs` ora copre inizializzazione della data odierna su GET register e rifiuto delle date future su POST register.
- Lo smoke Playwright ha rilevato che `_ValidationScriptsPartial` caricava `jquery.validate` senza `jquery`; la partial e stata corretta aggiungendo `jquery.min.js` prima dei plugin di validazione.
- Smoke desktop: data iniziale `2026-04-17`, pulsante `Crea account` inizialmente disabilitato, 9 label obbligatorie con `*`, header `* campi obbligatori`, `Password` e `Ripeti password` sulla stessa riga.
- Smoke compilazione campi: dopo valorizzazione di tutti i campi obbligatori il pulsante si abilita.
- Smoke mobile 390x844: nessuno scroll orizzontale, password e ripeti password impilati e allineati.
- Smoke toast errore: sfondo `rgb(220, 38, 38)`, testo bianco, font-weight `700`, chiusura al click e auto-close confermata.

### File Modificati

- `DashboardOrders.Tests/AccountControllerTests.cs`: aggiunti test su data odierna iniziale e data futura non valida.
- `Views/Shared/_ValidationScriptsPartial.cshtml`: aggiunto `jquery.min.js` prima degli script di validazione.
- `wwwroot/css/app.css`: rigenerato con `npm run build:css`.
- `docs/PLAN.md`: aggiornata esecuzione della Fase 3.

### Validazione Eseguita

- `dotnet test DashBoard01.sln`: superato con 54 test passati, 0 falliti, 0 ignorati.
- `dotnet build DashBoard01.sln`: superato con 0 warning e 0 errori.
- `npm run build:css`: superato; resta warning informativo `caniuse-lite is outdated`.
- Smoke Playwright su `http://localhost:5107/Account/Register`: stato iniziale, desktop/mobile, abilitazione submit e toast verificati.

### Decisioni Prese

- La correzione di `_ValidationScriptsPartial` rientra nella fase verifiche perche emersa da errore console sulla pagina di registrazione.
- Non sono stati aggiunti test JavaScript persistenti; il comportamento client e stato verificato con smoke browser reale.

## Fase 4 - Revisione finale e archiviazione documenti

### Obiettivo

Rivedere il lavoro, aggiornare il piano con l'esito finale e archiviare PRD/PLAN a sviluppo completato.

### Stato

Completata.

### Attivita

- [x] Rivedere diff dei file modificati.
- [x] Aggiornare `docs/PLAN.md` con file modificati, validazioni, decisioni, rischi residui e prossimo step.
- [x] Verificare `git status --short`.
- [x] Creare `docs/History` se assente.
- [x] Archiviare `docs/PRD.md` e `docs/PLAN.md` con timestamp comune.

### File o Aree Coinvolte

- `docs/PLAN.md`
- `docs/PRD.md`
- `docs/History/`
- Tutti i file modificati dal task.

### Validazioni

- Controllo diff.
- Controllo stato git.
- Conferma presenza archivi PRD/PLAN.

### Definition of Done

- Piano aggiornato con esito reale.
- PRD e PLAN archiviati in `docs/History`.
- Riepilogo finale pronto con file toccati, verifiche eseguite e rischi residui.

### Note

- Non archiviare i documenti prima che tutte le fasi tecniche siano completate e validate.
- `git diff --check` superato; presenti solo warning Git sui fine riga LF/CRLF.
- La revisione diff conferma perimetro coerente con pagina registrazione, toast condivisi, script client, test controller e documentazione.

### File Modificati

- `Controllers/AccountController.cs`: inizializzazione data odierna e validazione date future.
- `DashboardOrders.Tests/AccountControllerTests.cs`: test per data odierna iniziale e data futura non valida.
- `Views/Account/Register.cshtml`: layout registrazione, label obbligatorie, header card, submit disabilitato.
- `Views/Shared/_ToastMessages.cshtml`: toast cliccabili, auto-close e stile errore.
- `Views/Shared/_ValidationScriptsPartial.cshtml`: jQuery caricato prima dei plugin di validazione.
- `wwwroot/js/site.js`: inizializzazione ricerca, toast e form registrazione.
- `wwwroot/css/app.css`: CSS Tailwind rigenerato.
- `docs/PRD.md`: source of truth del task.
- `docs/PLAN.md`: piano ed esito finale.

### Validazione Eseguita

- `git status --short`: verificato prima della revisione finale.
- `git diff --check`: superato.
- `dotnet test DashBoard01.sln`: superato con 54 test passati, 0 falliti, 0 ignorati.
- `dotnet build DashBoard01.sln`: primo tentativo fallito per file lock causato da esecuzione parallela con `dotnet test`; rilancio sequenziale superato con 0 warning e 0 errori.
- `npm run build:css`: superato; resta warning informativo `caniuse-lite is outdated`.
- Smoke Playwright gia completato nella Fase 3 su `http://localhost:5107/Account/Register`.

### Decisioni Prese

- Nessuna nuova dipendenza introdotta.
- Il comportamento client non sostituisce la validazione server.
- I documenti PRD/PLAN vengono archiviati a chiusura del piano complessivo.

### Rischi Residui

- Non esiste una suite JavaScript/Playwright persistente; il comportamento client e stato validato tramite smoke browser reale.
- `npm run build:css` continua a mostrare il warning informativo `caniuse-lite is outdated`.

## Registro Decisioni

- 2026-04-17: il task viene gestito con `dotnet-task-decomposition` perche la richiesta include PRD/PLAN, modifiche UI, comportamento client, toast condivisi e verifiche.
- 2026-04-17: il PRD/PLAN precedente su auth/JWT/CORS e considerato concluso e gia archiviato; i documenti correnti vengono riusati come source of truth del nuovo task.
- 2026-04-18: piano tecnico completato; PRD e PLAN archiviati in `docs/History`.

## Problemi Emersi

- 2026-04-17: rilevato conflitto funzionale tra requisito "data odierna selezionata" e validazione server attuale che richiede una data nel passato.
- 2026-04-18: primo `dotnet build DashBoard01.sln` della revisione finale fallito per file lock su `DashboardOrders.deps.json` per esecuzione parallela con `dotnet test`; rilancio sequenziale superato.

## Prossimo Step

Nessuno step tecnico residuo nel piano corrente.
