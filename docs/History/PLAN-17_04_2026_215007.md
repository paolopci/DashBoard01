# PLAN - Autenticazione, Autorizzazione, JWT e CORS

## Stato Generale

- PRD: aggiornato in `docs/PRD.md`.
- Piano: aggiornato in `docs/PLAN.md`.
- Fase corrente: Fase 6 completata. Piano tecnico completato.
- Regola operativa: procedere per sotto-task chiari e validabili, aggiornando il piano dopo ogni fase.

## Checklist Generale

- [x] Leggere `AGENTS.md` e moduli `_shared`.
- [x] Leggere `docs/PRD.md` e `docs/PLAN.md` esistenti.
- [x] Applicare la skill `dotnet-task-decomposition`.
- [x] Creare PRD e PLAN per auth/JWT/CORS.
- [x] Completare Fase 1 - Baseline, pacchetti e schema Identity.
- [x] Completare Fase 2 - Configurazione centralizzata Identity, JWT e CORS.
- [x] Completare Fase 3 - Model, DTO e servizio autenticazione.
- [x] Completare Fase 4 - Controller MVC, pagine Tailwind e toast.
- [x] Completare Fase 5 - Navbar, autorizzazione e gestione errori.
- [x] Completare Fase 6 - Test automatici e verifiche finali.
- [x] Archiviare PRD/PLAN in `docs/History` a sviluppo complessivo concluso.

## Fase 1 - Baseline, Pacchetti e Schema Identity

### Obiettivo

Preparare le dipendenze e il modello dati Identity senza rompere lo schema applicativo esistente.

### Stato

Completata e validata.

### Attivita

- [x] Verificare `git status --short`.
- [x] Aggiungere package Identity/JWT necessari, evitando AutoMapper se non necessario.
- [x] Creare `ApplicationUser` con campi profilo richiesti.
- [x] Estendere `DashboardOrdersDbContext` da `IdentityDbContext<ApplicationUser>`.
- [x] Verificare e, se serve, creare script SQL idempotente per colonne profilo mancanti su `AspNetUsers`.

### File o Aree Coinvolte

- `DashboardOrders.csproj`
- `Data/DashboardOrdersDbContext.cs`
- `Data/Entities/`
- `scripts/`

### Validazioni

- `dotnet restore DashBoard01.sln`
- `dotnet build DashBoard01.sln`
- Query read-only su schema `AspNetUsers` se necessario.

### Definition of Done

- Il progetto compila con Identity registrabile su EF Core.
- I campi profilo richiesti sono rappresentati nel modello utente.

### File Modificati

- `DashboardOrders.csproj`: aggiunti `Microsoft.AspNetCore.Authentication.JwtBearer` e `Microsoft.AspNetCore.Identity.EntityFrameworkCore` versione `9.0.12`.
- `Data/Entities/ApplicationUser.cs`: aggiunta entity utente applicativa basata su `IdentityUser`.
- `Data/DashboardOrdersDbContext.cs`: esteso `IdentityDbContext<ApplicationUser>` e configurati campi profilo.
- `scripts/2026-04-17-prepare-identity-profile.sql`: aggiunto script idempotente per colonne profilo e indice `FiscalCode`.

### Validazione Eseguita

- `git status --short`: verificato prima delle modifiche operative.
- `dotnet build DashBoard01.sln`: superato con 0 warning e 0 errori dopo l'aggiunta di Identity/JWT.
- Script SQL Identity applicato su `DashboardAppDb` tramite `sqlcmd` nel container `sql-container`.
- Query schema `AspNetUsers`: confermate colonne `FirstName`, `LastName`, `DateOfBirth`, `City`, `Country`, `FiscalCode` e indice `IX_AspNetUsers_FiscalCode`.

### Problemi Emersi nella Fase

- Una build iniziale e stata bloccata dal processo locale `DashboardOrders.exe` in esecuzione; il processo e stato fermato e la build successiva e passata.
- Lo script SQL ha richiesto SQL dinamico per creare l'indice su una colonna aggiunta nello stesso batch.
- SQL Server ha richiesto `ANSI_NULLS ON` e `QUOTED_IDENTIFIER ON` per l'indice filtrato.

## Fase 2 - Configurazione Centralizzata Identity, JWT e CORS

### Obiettivo

Spostare configurazione Identity, JWT e CORS fuori da `Program.cs` in extension method dedicati.

### Stato

Completata e validata.

### Attivita

- [x] Creare cartella `Extensions/Auth`.
- [x] Creare opzioni JWT e CORS.
- [x] Creare extension per `AddDashboardIdentity`, `AddDashboardJwtAuthentication`, `AddDashboardCors`.
- [x] Aggiornare `Program.cs` con chiamate essenziali e ordine middleware corretto.
- [x] Aggiornare `appsettings*.json` solo con valori non segreti.

### File o Aree Coinvolte

- `Program.cs`
- `Extensions/Auth/`
- `appsettings.json`
- `appsettings.Development.json`

### Validazioni

- `dotnet build DashBoard01.sln`
- Revisione ordine middleware: `UseCors`, `UseAuthentication`, `UseAuthorization`.

### Definition of Done

- `Program.cs` non contiene configurazione inline di CORS o token JWT.
- La pipeline supporta cookie Identity e JWT bearer.

### File Modificati

- `Extensions/Auth/CorsOptions.cs`: aggiunte opzioni CORS.
- `Extensions/Auth/CorsExtensions.cs`: aggiunte extension `AddDashboardCors` e `UseDashboardCors`.
- `Extensions/Auth/IdentityExtensions.cs`: aggiunta configurazione Identity, cookie applicativo e authorization.
- `Extensions/Auth/JwtOptions.cs`: aggiunte opzioni JWT.
- `Extensions/Auth/JwtExtensions.cs`: aggiunta configurazione JWT bearer con fallback solo Development.
- `Program.cs`: sostituita configurazione inline con extension e aggiunti `UseDashboardCors`, `UseAuthentication`, `UseAuthorization`.
- `appsettings.json`: aggiunte impostazioni non segrete per `Jwt` e `Cors`.
- `appsettings.Development.json`: aggiunte impostazioni non segrete per `Jwt` e origini CORS locali.

### Validazione Eseguita

- `dotnet build DashBoard01.sln`: superato con 0 warning e 0 errori.
- Revisione pipeline: `UseDashboardCors()` e eseguito dopo `UseRouting()` e prima di `UseAuthentication()`/`UseAuthorization()`.
- Scansione configurazione versionata: nessuna connection string o password aggiunta in `appsettings.json` o `appsettings.Development.json`.

### Decisioni Prese

- AutoMapper non e stato introdotto perche non serve alla configurazione Identity/JWT/CORS.
- La chiave JWT reale resta fuori dai file versionati; in Development viene usato un fallback esplicito solo per avvio locale.
- Se `Cors:AllowedOrigins` e vuoto, la policy non accetta origini cross-origin invece di usare `AllowAnyOrigin`.

## Fase 3 - Model, DTO e Servizio Autenticazione

### Obiettivo

Implementare contratti e logica applicativa per registrazione, login e logout.

### Stato

Completata e validata.

### Attivita

- [x] Creare model `Register`, `Login`, `Logout`.
- [x] Creare DTO `RegisterDto`, `LoginDto`, `LogoutDto` e DTO risultato.
- [x] Implementare servizio auth con `UserManager`, `SignInManager` e generazione JWT.
- [x] Gestire controlli utente esistente e validazioni input.
- [x] Centralizzare messaggi e risultato operazione.

### File o Aree Coinvolte

- `Models/`
- `Services/`
- `Extensions/Auth/`

### Validazioni

- `dotnet build DashBoard01.sln`
- Test unitari mirati nella Fase 6.

### Definition of Done

- Il servizio auth espone registrazione, login e logout con risultati espliciti e testabili.

### File Modificati

- `Models/Register.cs`: aggiunto model registrazione con data annotations per nome, cognome, email, data di nascita, citta, paese, codice fiscale, password e ripetizione password.
- `Models/Login.cs`: aggiunto model login con campo `UserLogin` esposto come `Login` lato UI e password.
- `Models/Logout.cs`: aggiunto model logout con `ReturnUrl` opzionale.
- `Models/RegisterDto.cs`: aggiunto DTO registrazione.
- `Models/LoginDto.cs`: aggiunto DTO login.
- `Models/LogoutDto.cs`: aggiunto DTO logout.
- `Models/AccountOperationResult.cs`: aggiunto risultato centralizzato per successo, errore, messaggi e token.
- `Services/IAccountService.cs`: aggiunto contratto per registrazione, login e logout.
- `Services/AccountService.cs`: implementata logica con `UserManager<ApplicationUser>`, `SignInManager<ApplicationUser>` e generazione JWT.
- `Extensions/Auth/JwtOptions.cs`: aggiunto resolver condiviso per opzioni JWT e fallback Development.
- `Extensions/Auth/JwtExtensions.cs`: riusato resolver condiviso per evitare duplicazione configurazione token.
- `Program.cs`: registrato `IAccountService`.

### Validazione Eseguita

- `git status --short`: verificato prima delle modifiche dello step.
- `dotnet build DashBoard01.sln`: superato con 0 warning e 0 errori.
- Ricerca statica dei nuovi contratti: confermati model, DTO, `IAccountService`, `AccountService` e registrazione DI.

### Decisioni Prese

- Il campo richiesto `Cap` viene trattato come codice fiscale nel model UI e mappato a `FiscalCode` nel DTO/utente applicativo.
- La registrazione non esegue login automatico: restituisce successo e messaggio per accedere.
- Il login crea cookie Identity tramite `SignInManager` e produce anche token JWT nel risultato del servizio.
- I messaggi utente sono centralizzati in `AccountOperationResult`; errori Identity vengono propagati come elenco testabile.

### Rischi Residui

- Le validazioni UI saranno applicate nel controller/view della Fase 4; il servizio copre input nulli, email mancante, utente esistente, password non coincidenti e credenziali non valide.
- I test automatici specifici del servizio sono previsti in Fase 6.

## Fase 4 - Controller MVC, Pagine Tailwind e Toast

### Obiettivo

Creare l'esperienza utente per registrazione, login e logout.

### Stato

Completata e validata.

### Attivita

- [x] Creare `AccountController`.
- [x] Creare view `Register`, `Login`, `Logout` con Tailwind e antiforgery.
- [x] Creare partial toast Tailwind basata su `TempData`.
- [x] Usare redirect PRG con messaggi di successo/errore.
- [x] Verificare layout mobile e desktop.

### File o Aree Coinvolte

- `Controllers/AccountController.cs`
- `Views/Account/`
- `Views/Shared/`
- `Styles/app.css`
- `wwwroot/css/app.css`

### Validazioni

- `dotnet build DashBoard01.sln`
- `npm run build:css`
- Smoke HTTP pagine account.

### Definition of Done

- Le tre pagine auth sono navigabili, validate e coerenti con il layout.

### File Modificati

- `Controllers/AccountController.cs`: aggiunte action GET/POST per `Register`, `Login`, `Logout` e `AccessDenied`.
- `Views/Account/Register.cshtml`: aggiunta pagina registrazione responsive con Tailwind, tag helper e validation summary.
- `Views/Account/Login.cshtml`: aggiunta pagina login responsive con Tailwind, tag helper e validation summary.
- `Views/Account/Logout.cshtml`: aggiunta pagina conferma logout.
- `Views/Account/AccessDenied.cshtml`: aggiunta pagina accesso negato.
- `Views/Shared/_ToastMessages.cshtml`: aggiunta partial toast Tailwind basata su `TempData`.
- `Views/Shared/_Layout.cshtml`: inclusa partial toast senza modificare la navigazione.
- `wwwroot/css/app.css`: rigenerato con `npm run build:css`.

### Validazione Eseguita

- `git status --short`: verificato prima delle modifiche dello step.
- `dotnet build DashBoard01.sln`: superato con 0 warning e 0 errori.
- `npm run build:css`: superato; resta solo warning informativo `caniuse-lite is outdated`.
- Ricerca statica su form account: confermati form/link `Register`, `Login` e `Logout`.

### Decisioni Prese

- I token JWT generati dal login non vengono mostrati o salvati nei toast.
- `TempData["Toast.Success"]` e `TempData["Toast.Error"]` sono usati come contratto semplice per messaggi utente.
- `AccountController` accetta `returnUrl` solo se locale tramite `Url.IsLocalUrl`.

### Problemi Emersi nella Fase

- Primo build della fase fallito per conflitto tra action `Register()` e tipo `Register` nei `nameof`; corretto usando `nameof(Models.Register.DateOfBirth)`.
- Una ricerca `rg` e fallita per quoting della regex in PowerShell; nessun impatto sul codice.

### Rischi Residui

- La navbar non mostra ancora login/logout/registrazione in base allo stato utente; previsto in Fase 5.
- I test automatici di `AccountController` sono previsti in Fase 6.

## Fase 5 - Navbar, Autorizzazione e Gestione Errori

### Obiettivo

Integrare stato autenticazione nel layout e rendere la gestione errori coerente.

### Stato

Completata e validata.

### Attivita

- [x] Aggiornare `_Layout.cshtml` con pulsanti condizionati.
- [x] Applicare `[Authorize]` dove necessario e `[AllowAnonymous]` sulle pagine pubbliche/account.
- [x] Centralizzare gestione eccezioni e messaggi utente.
- [x] Verificare logout da navbar.
- [x] Verificare CORS/JWT su endpoint token o servizio.

### File o Aree Coinvolte

- `Views/Shared/_Layout.cshtml`
- `Controllers/`
- `Extensions/`

### Validazioni

- `dotnet build DashBoard01.sln`
- Smoke HTTP autenticato/non autenticato.

### Definition of Done

- Il layout riflette correttamente lo stato utente e gli errori utente non espongono dettagli interni.

### File Modificati

- `Controllers/HomeController.cs`: aggiunto `[Authorize]` sul controller e action `Error` anonima con `ErrorViewModel`.
- `Controllers/CategoryController.cs`: aggiunto `[Authorize]` sul controller categorie.
- `Program.cs`: centralizzata gestione errori con `UseExceptionHandler` e `UseStatusCodePagesWithReExecute`.
- `Views/Shared/_Layout.cshtml`: aggiunti pulsanti condizionati `Logout` oppure `Login`/`Registrati`, con form POST per logout.
- `Views/Shared/Error.cshtml`: sostituita view errore con messaggio utente non sensibile.
- `wwwroot/css/app.css`: rigenerato con `npm run build:css`.
- `AGENTS.md`: aggiunta retrospettiva per evitare errori ripetuti di quoting `rg` in PowerShell.

### Validazione Eseguita

- `git status --short`: verificato prima delle modifiche dello step.
- `dotnet build DashBoard01.sln`: superato con 0 warning e 0 errori.
- `npm run build:css`: superato; resta solo warning informativo `caniuse-lite is outdated`.
- Smoke HTTP su `http://localhost:5105`:
  - `/`: `302` verso `/Account/Login?ReturnUrl=%2F`.
  - `/Account/Login`: `200`.
  - `/Account/Register`: `200`.
  - `/Account/Logout`: `302` verso login se anonimo.
  - `/Account/AccessDenied`: `403`.
  - `/Home/Error?statusCode=500`: `500` con messaggio centralizzato.
  - `/Home/Error?statusCode=404`: `404` con messaggio centralizzato.
- Server locale avviato per smoke test e poi fermato.

### Decisioni Prese

- Le aree dashboard e categorie richiedono autenticazione.
- Le pagine account necessarie restano anonime; logout richiede autenticazione.
- Il logout dalla navbar usa POST con antiforgery invece di un link GET.
- Gli errori centralizzati non mostrano stack trace o dettagli tecnici sensibili.

### Problemi Emersi nella Fase

- Lo stesso errore di quoting con `rg` in PowerShell e stato ripetuto; come richiesto, e stata eseguita retrospettiva e aggiornata la regola locale in `AGENTS.md`.

### Rischi Residui

- Manca ancora copertura automatica per redirect auth, controller account e servizio auth; prevista in Fase 6.
- La smoke HTTP non esegue ancora registrazione/login reali via form; previsto nella verifica finale.

## Fase 6 - Test Automatici e Verifiche Finali

### Obiettivo

Validare il flusso auth e chiudere il lavoro con build/test/smoke.

### Stato

Completata e validata.

### Attivita

- [x] Aggiungere test xUnit per servizio auth.
- [x] Aggiungere test xUnit per `AccountController`.
- [x] Eseguire `dotnet test DashBoard01.sln`.
- [x] Eseguire `dotnet build DashBoard01.sln`.
- [x] Eseguire `npm run build:css`.
- [x] Avviare app e verificare pagine principali/account.
- [x] Archiviare PRD/PLAN in `docs/History`.

### File o Aree Coinvolte

- `DashboardOrders.Tests/`
- `docs/History/`

### Validazioni

- `dotnet test DashBoard01.sln`
- `dotnet build DashBoard01.sln`
- `npm run build:css`
- Smoke HTTP locale.

### Definition of Done

- Tutti i controlli pertinenti passano e PRD/PLAN sono archiviati.

### File Modificati

- `DashboardOrders.Tests/AccountServiceTests.cs`: aggiunti test su registrazione OK, input null, utente esistente, login OK con token, login null e logout.
- `DashboardOrders.Tests/AccountControllerTests.cs`: aggiunti test su GET/POST register, login, logout, errori e access denied.
- `Controllers/AccountController.cs`: rafforzata validazione manuale login e fallback errori `ModelState`.
- `docs/PLAN.md`: aggiornata chiusura del piano.
- `docs/History/PRD-17_04_2026_215007.md`: archivio finale PRD.
- `docs/History/PLAN-17_04_2026_215007.md`: archivio finale PLAN.

### Validazione Eseguita

- `git status --short`: verificato prima delle modifiche dello step.
- `dotnet test DashBoard01.sln`: superato con 52 test passati, 0 falliti, 0 ignorati.
- `dotnet build DashBoard01.sln`: superato con 0 warning e 0 errori.
- `npm run build:css`: superato; resta solo warning informativo `caniuse-lite is outdated`.
- Smoke HTTP finale su `http://localhost:5106` con sessione e antiforgery:
  - home anonima: `302` verso login.
  - registrazione nuovo utente: `302` verso login.
  - login nuovo utente: `302` verso home.
  - home autenticata: `200` e contenuto dashboard presente.
  - pagina logout autenticata: `200`.
  - logout POST: `302` verso home.
  - home dopo logout: `302` verso login.
- Server locale avviato per smoke test e poi fermato.

### Decisioni Prese

- I test del servizio usano `NSubstitute` su `UserManager<ApplicationUser>` e `SignInManager<ApplicationUser>` per restare unitari e non dipendere dal database.
- Lo smoke finale usa form reali con antiforgery per validare il flusso browser essenziale senza introdurre test end-to-end persistenti.

### Problemi Emersi nella Fase

- I test hanno rilevato un bug reale su `Login` POST con model nullo e su fallimento servizio senza errori dettagliati; `AccountController` e stato corretto con `ValidateLogin` e fallback `ModelState`.
- PowerShell emette messaggi quando `Invoke-WebRequest` usa `MaximumRedirection 0` su redirect attesi; gli status e le location sono stati comunque raccolti e validati.

### Rischi Residui

- Non e stata introdotta una suite browser Playwright persistente; lo smoke finale e stato eseguito via HTTP.
- La configurazione JWT di produzione richiede ancora una `Jwt:SecretKey` esterna e sicura.

## Registro Decisioni

- 2026-04-17: il task viene gestito con `dotnet-task-decomposition` perche include backend, frontend, configurazione, sicurezza, test e documentazione.
- 2026-04-17: AutoMapper non viene introdotto perche i mapping richiesti sono semplici e il perimetro non giustifica una dipendenza aggiuntiva.
- 2026-04-17: la sessione MVC usa cookie Identity; il token JWT viene generato dal servizio login per integrazioni API.
- 2026-04-17: test controller e service aggiunti con xUnit, FluentAssertions e NSubstitute.
- 2026-04-17: piano tecnico completato; PRD e PLAN archiviati in `docs/History`.

## Problemi Emersi

- 2026-04-17: dopo la prima risposta operativa e stato rilevato che il workflow locale `1/2` non era stato rispettato. Il lavoro successivo procede con conferme step/item.
- 2026-04-17: build bloccata da processo locale `DashboardOrders.exe`; risolta fermando il processo.
- 2026-04-17: script SQL Identity corretto per compilazione batch e opzioni richieste da indice filtrato.
- 2026-04-17: errore di quoting `rg` in PowerShell ripetuto due volte; aggiornata retrospettiva in `AGENTS.md`.

## Prossimo Step

Nessuno step tecnico residuo nel piano corrente.
