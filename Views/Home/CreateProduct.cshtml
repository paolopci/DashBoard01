@model DashboardOrders.Models.ProductFormViewModel
@{
    ViewData["Title"] = "Nuovo prodotto";
}

<div class="mb-8">
    <h2 class="text-2xl font-bold text-slate-800">Nuovo prodotto</h2>
    <p class="mt-1 text-slate-500">Inserisci un prodotto nel catalogo.</p>
</div>
 <div class="mt-3 inline-flex items-center rounded-lg border border-indigo-100 bg-indigo-50 px-3 py-2 text-sm text-indigo-700">
        Demo PR: questa etichetta è stata aggiunta per la prova di Pull Request.
        Demo PR: questa etichetta è stata aggiunta per la prova di Pull Request.
        Demo PR: questa etichetta è stata aggiunta per la prova di Pull Request.
        Demo PR: questa etichetta è stata aggiunta per la prova di Pull Request.
        Demo PR: questa etichetta è stata aggiunta per la prova di Pull Request.
    </div>
<partial name="_ProductForm" model="Model" />

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
